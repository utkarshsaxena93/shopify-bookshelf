<h5 class="table__heading">Your reading list:</h5>
<table class="table table-striped currentlyReadingList">
  <thead>
    <tr>
      <th class="table__subheading">ISBN</th>
      <th class="table__subheading">Title</th>
      <% if @user.id == current_user.id %>
        <th class="table__subheading">Actions</th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% obj.each do |book| %>
      <tr>
        <td><%= book.isbn %></td>
        <td><%= link_to book.title, book_path(book) %></td>
        <% if @user.id == current_user.id %>
          <td>
            <%= link_to 'Mark as completed.', remove_from_currently_reading_path(book), :method => :delete,
             :data => { :confirm => "Are you sure?"},
             :class => "btn btn-xs btn-success" %>
          </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
