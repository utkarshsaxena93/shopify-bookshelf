<%= render 'shared/errors', obj: @book %>

<div class="row">
  <div class="col-lg-12">
    <%= form_for @book, :html => { :class => "form-horizontal book", role: "form" } do |f| %>

      <div class="form-group form-group-isbn">
        <%= f.label :isbn, :class => 'control-label col-lg-2' %>
        <div class="col-lg-10">
          <%= f.number_field :isbn, :class => 'form-control', autofocus: true %>
        </div>
      </div>
      <span>
  Hello, <strong bind="newBookInfo[0].id"></strong>
</span>
      <%= f.hidden_field :googleid, bind: "newBookInfo[0].id" %>
      <%= f.hidden_field :category, bind: "newBookInfo[1].id" %>

      <div class="form-group">
        <div class="col-lg-offset-2 col-lg-10">
          <%= f.submit nil, :class => 'btn btn-primary', disabled: true %>
          <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                    books_path, :class => 'btn btn-default' %>
        </div>
      </div>

    <% end %>
  </div>
</div>
